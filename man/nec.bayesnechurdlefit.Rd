% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesnechurdlefit-class.R
\name{nec.bayesnechurdlefit}
\alias{nec.bayesnechurdlefit}
\title{Extracts the predicted NEC value from a \code{\link{bayesnechurdlefit}}}
\usage{
\method{nec}{bayesnechurdlefit}(
  object,
  posterior = FALSE,
  xform = identity,
  prob_vals = c(0.5, 0.025, 0.975),
  which = "combined",
  ...
)
}
\arguments{
\item{object}{An object of class \code{\link{bayesnechurdlefit}}.}

\item{posterior}{A \code{\link[base]{logical}} value indicating if the full
posterior sample of calculated NEC values should be returned instead of
just the median and 95\% credible intervals.}

\item{xform}{A function to apply to the returned estimated concentration
values.}

\item{prob_vals}{A vector indicating the probability values over which to
return the estimated NEC value. Defaults to 0.5 (median) and 0.025 and
0.975 (95 percent credible intervals).}

\item{which}{Which component to return: \code{"combined"} (the default),
\code{"growth"} or \code{"survival"}.}

\item{...}{Additional arguments passed to methods.}
}
\value{
A vector containing the estimated NEC value, including upper and
lower credible interval bounds.
}
\description{
Extracts the predicted NEC value from a \code{\link{bayesnechurdlefit}}
}
\details{
The combined no-effect concentration is
\code{pmin(nec_growth, nec_survival)} evaluated per posterior draw. Below
both thresholds the growth curve sits at \code{top} and the survival curve at
its own control value, so their product is flat; it departs that plateau at
whichever threshold binds first.

This is exact when both components are threshold (\code{nec}-type) models.
Where either component is an \code{ecx}-type model, or a model-averaged fit
containing them, the corresponding value is an NSEC and the combination
should be read as approximate -- a message is emitted in that case.
}
