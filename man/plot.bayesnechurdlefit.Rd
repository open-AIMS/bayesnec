% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesnechurdlefit-methods.R
\name{plot.bayesnechurdlefit}
\alias{plot.bayesnechurdlefit}
\title{Plots a \code{\link{bayesnechurdlefit}}}
\usage{
\method{plot}{bayesnechurdlefit}(
  x,
  ...,
  which = "combined",
  CI = TRUE,
  add_nec = TRUE,
  resolution = 1000,
  x_range = NA,
  xform = identity,
  ylab = NULL,
  xlab = "Predictor"
)
}
\arguments{
\item{x}{An object of class \code{\link{bayesnechurdlefit}}.}

\item{...}{Further arguments to \code{\link[graphics]{plot}}.}

\item{which}{Which curve to plot: \code{"combined"} (the default),
\code{"growth"}, \code{"survival"}, or \code{"all"} for three panels.}

\item{CI}{Should credible intervals be drawn? Defaults to \code{TRUE}.}

\item{add_nec}{Should the no-effect estimate be drawn? Defaults to
\code{TRUE}.}

\item{resolution}{The number of unique predictor values to predict over.}

\item{x_range}{A range of predictor values to plot over.}

\item{xform}{A function to apply to the predictor.}

\item{ylab}{Y axis label. Defaults to a label describing \code{which}.}

\item{xlab}{X axis label.}
}
\value{
A plot.
}
\description{
Plots a \code{\link{bayesnechurdlefit}}
}
\details{
The default plots the combined endpoint only -- the expected
response per individual exposed -- because that is the quantity a hurdle
model exists to estimate. \code{which = "all"} adds the two components as
separate panels; they are not overlaid because the growth curve is in
response units and the survival curve is a probability, so sharing one pair
of axes would misrepresent their relative magnitude.
}
