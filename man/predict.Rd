% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bnecfit-methods.R, R/bayesnecfit-methods.R,
%   R/bayesmanecfit-methods.R
\name{predict.bnecfit}
\alias{predict.bnecfit}
\alias{predict.bayesnecfit}
\alias{predict.bayesmanecfit}
\title{Generates predictions for objects fitted by \code{\link{bnec}}}
\usage{
\method{predict}{bnecfit}(object, ...)

\method{predict}{bayesnecfit}(object, ...)

\method{predict}{bayesmanecfit}(object, summary = TRUE, robust = FALSE, probs = c(0.025, 0.975), ...)
}
\arguments{
\item{object}{An object of class \code{\link{bayesnecfit}} or
\code{\link{bayesmanecfit}}.}

\item{...}{Additional arguments to \code{\link[brms]{posterior_predict}}.}

\item{summary}{Should summary statistics be returned
instead of the raw values? Default is \code{TRUE}.}

\item{robust}{If \code{FALSE} (the default) the mean is used as
the measure of central tendency and the standard deviation as
the measure of variability. If \code{TRUE}, the median and the
median absolute deviation (MAD) are applied instead.
Only used if \code{summary} is \code{TRUE}.}

\item{probs}{The percentiles to be computed by the \code{quantile}
function. Only used if \code{summary} is \code{TRUE}.}
}
\value{
See \code{?}\code{\link[brms]{predict.brmsfit}}.
}
\description{
Generates predictions for objects fitted by \code{\link{bnec}}
}
\section{Methods (by class)}{
\itemize{
\item \code{predict(bnecfit)}: Serves as a wrapper which redirects the function
depending on whether \code{object} is of class \code{\link{bayesnecfit}} or
\code{\link{bayesmanecfit}}.

\item \code{predict(bayesnecfit)}: Generates predictions for \code{\link{bayesnecfit}}
objects fitted by \code{\link{bnec}}.

\item \code{predict(bayesmanecfit)}: Generates predictions for \code{\link{bayesmanecfit}}
objects fitted by \code{\link{bnec}}.

}}
