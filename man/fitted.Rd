% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bnecfit-methods.R, R/bayesnecfit-methods.R,
%   R/bayesmanecfit-methods.R
\name{fitted.bnecfit}
\alias{fitted.bnecfit}
\alias{fitted.bayesnecfit}
\alias{fitted.bayesmanecfit}
\title{Generates mean posterior predictions for objects fitted by
\code{\link{bnec}}}
\usage{
\method{fitted}{bnecfit}(object, ...)

\method{fitted}{bayesnecfit}(object, summary = TRUE, robust = FALSE, probs = c(0.025, 0.975), ...)

\method{fitted}{bayesmanecfit}(object, summary = TRUE, robust = FALSE, probs = c(0.025, 0.975), ...)
}
\arguments{
\item{object}{An object of class \code{\link{bayesnecfit}} or
\code{\link{bayesmanecfit}}.}

\item{...}{Additional arguments to \code{\link[brms]{posterior_epred}}.}

\item{summary}{Should summary statistics be returned
instead of the raw values? Default is \code{TRUE}.}

\item{robust}{If \code{FALSE} (the default) the mean is used as
the measure of central tendency and the standard deviation as
the measure of variability. If \code{TRUE}, the median and the
median absolute deviation (MAD) are applied instead.
Only used if \code{summary} is \code{TRUE}.}

\item{probs}{The percentiles to be computed by the \code{quantile}
function. Only used if \code{summary} is \code{TRUE}.}
}
\value{
See \code{?}\code{\link[brms]{fitted.brmsfit}}.
}
\description{
Generates mean posterior predictions for objects fitted by
\code{\link{bnec}}
}
\section{Methods (by class)}{
\itemize{
\item \code{fitted(bnecfit)}: Serves as a wrapper which redirects the function
depending on whether \code{object} is of class \code{\link{bayesnecfit}} or
\code{\link{bayesmanecfit}}.

\item \code{fitted(bayesnecfit)}: Generates mean posterior predictions for
\code{\link{bayesnecfit}} objects fitted by \code{\link{bnec}}.

\item \code{fitted(bayesmanecfit)}: Generates mean posterior predictions for
\code{\link{bayesmanecfit}} objects fitted by \code{\link{bnec}}.

}}
